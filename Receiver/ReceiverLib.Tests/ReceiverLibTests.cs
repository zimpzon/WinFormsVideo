namespace ReceiverLib.Tests;

public class ReceiverLibTests
{
    // Placeholder so the project builds and `dotnet test` has something to discover.
    // Real tests arrive when ReceiverLib gets its architecture skeleton.
    [Fact]
    public void Placeholder()
    {
        Assert.True(true);
    }
}
